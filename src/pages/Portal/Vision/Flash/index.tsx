import React, { FC } from "react";
import View from "../../../../components/View";

const Flash: FC = () => {
  return <View {...category} />;
};

const category = {
  title: "Flash Updates",
  categoryId: "0115",
};

export default Flash;
