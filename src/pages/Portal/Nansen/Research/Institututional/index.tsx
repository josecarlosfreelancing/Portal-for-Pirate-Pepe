import React, { FC } from "react";
import View from "../../../../../components/View";

const Institutional: FC = () => {
  return <View {...category} />;
};

const category = {
  title: "Institutional",
  categoryId: "030205",
};

export default Institutional;
