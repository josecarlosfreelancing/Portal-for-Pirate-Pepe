import React, { FC } from "react";
import View from "../../../../../components/View";

const Alpha: FC = () => {
  return <View {...category} />;
};

const category = {
  title: "Alpha",
  categoryId: "030206",
};

export default Alpha;
