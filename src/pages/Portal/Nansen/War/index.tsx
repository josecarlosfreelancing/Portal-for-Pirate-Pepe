import React, { FC } from "react";
import View from "../../../../components/View";

const War: FC = () => {
  return <View {...category} />;
};

const category = {
  title: "War Room Notes",
  categoryId: "0303",
};

export default War;
