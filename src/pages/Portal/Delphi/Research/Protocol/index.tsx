import React, { FC } from "react";
import View from "../../../../../components/View";

const ProtocolDives: FC = () => {
  return <View {...category} />;
};

const category = {
  title: "Protocol Dives",
  categoryId: "040103",
};

export default ProtocolDives;
