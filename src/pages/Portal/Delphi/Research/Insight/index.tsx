import React, { FC } from "react";
import View from "../../../../../components/View";

const MarketInsight: FC = () => {
  return <View {...category} />;
};

const category = {
  title: "Market Insights",
  categoryId: "040107",
};

export default MarketInsight;
