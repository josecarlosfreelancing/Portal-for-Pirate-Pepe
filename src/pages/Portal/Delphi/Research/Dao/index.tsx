import React, { FC } from "react";
import View from "../../../../../components/View";

const DaoInsight: FC = () => {
  return <View {...category} />;
};

const category = {
  title: "DAO Insights",
  categoryId: "040105",
};

export default DaoInsight;
