import React, { FC } from "react";
import View from "../../../../../components/View";

const AnalysisDao: FC = () => {
  return <View {...category} />;
};

const category = {
  title: "DAO Activity & Coverage",
  categoryId: "040403",
};

export default AnalysisDao;
