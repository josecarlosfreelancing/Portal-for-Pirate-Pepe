import React, { FC } from "react";
import View from "../../../../../components/View";

const Gaming: FC = () => {
  return <View {...category} />;
};

const category = {
  title: "Gaming Sector",
  categoryId: "040402",
};

export default Gaming;
