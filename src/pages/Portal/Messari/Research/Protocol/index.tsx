import React, { FC } from "react";
import View from "../../../../../components/View";

const Protocol: FC = () => {
  return <View {...category} />;
};

const category = {
  title: "Procotol Reporting",
  categoryId: "020104",
};

export default Protocol;
