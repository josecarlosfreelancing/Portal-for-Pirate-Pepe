import React, { FC } from "react";
import View from "../../../../../components/View";

const Pro: FC = () => {
  return <View {...category} />;
};

const category = {
  title: "Pro Research",
  categoryId: "020102",
};

export default Pro;
